# Transformer-Diffusion-MNIST
A Transformer-based Denoiser for Diffusion Models trained on the MNIST dataset. The project implements a forward diffusion process with T=50 steps and a custom neural architecture combining convolutional encoders/decoders with multi-head self-attention blocks to iteratively reconstruct clean images from noise.
